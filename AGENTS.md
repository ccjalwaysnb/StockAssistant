# AGENTS.md —— 本仓库的工作规矩（与用户约定，2026-09-16 建立）

适用范围：本仓库（`D:\c ler\thridstep\炒股小助手`）。在这里做任何事之前，先读这份文件。

用户身份：学习者。学 C# 语法，也学怎么用 agent 干活。
所以这份文件既是纪律，也是"怎么配合他"的说明书。

---

## 1. 只读优先：没要求就不许动

用户没有明确说"改 / 写 / 重构 / 删"的时候，不要动任何文件、不写代码、不跑构建、不提交 git。

真要动手之前，先说清楚："准备改哪几个文件、改成什么样"。

## 2. 方案分歧以用户为准

用户给出的解决办法、逻辑顺序，即使我认为有更好的写法，也**先按用户的要求实现**。
实现完成之后，再附一段"我的想法"，说明差异和理由。
不要擅自把用户的方案替换成我自己的方案。

**如果我认为用户的意见本身有问题（看错了、或者照做会坏掉）：也先按他说的改，
改完再说明我为什么认为不行**，而不是先争论、先拖着不动手。

## 3. 没说清的地方自己拍板，但必须报账

遇到用户没讲清楚、且确实有多种合理做法的地方：自己挑一个先做，
但在交付说明里必须**单列"我拍脑袋定的部分"**，讲清楚：

- 选了哪个方案
- 为什么选它
- 备选是什么

## 4. 注释按"学习者标准"写足

注释要讲清楚：

- 为什么这么写（设计意图）
- 涉及哪些 C# / WPF 语法点
- 容易踩的坑

沿用仓库现有的注释风格：分区标题（A–G / ①–④）、编号、术语解释。

## 5. 不要主动做测试

用户在设计时就带了很强的防错思想——出错的地方不一定能亮出来，但至少不会整体崩溃。
所以：**除非用户明确要求，不要为了"验证一下"去编译、运行、联网试接口、写测试脚本。**

分析问题以读代码为主，把怀疑点直接讲出来就行。

## 6. K 线图相关代码：以我的判断为主

`CandleChart.xaml` / `CandleChart.xaml.cs` 不是用户自己写的，是他一步步协调 AI 写出来的，
他对其中的具体实现细节不完全清楚。

后续涉及 **K 线图的 bug 定位或代码删减**时，由我自行判断该怎么改，不必逐条请示他的设计意图。

---

## 术语表（用户定的叫法，沟通时一律按这套说）

| 说法 | 指什么 | 对应文件 / 控件 |
| --- | --- | --- |
| **盘口图** | 主界面上方那整块行情展示区：报价行（名称/代码/现价/涨跌）、7 个指标小格、买卖五档 | `MainPage.xaml` 的 ③ 行情区 |
| **K 图** | 主界面下方那块自绘蜡烛图 | `CandleChart.xaml` / `CandleChart.xaml.cs` |
| **实时区** | K 图下面那一行小字：盘口 / 日K / 分钟K 三个数据时间 | `MainPage.xaml` 的 `tQuote` / `tDaily` / `tMinute` |
| **心跳** | 0.5 秒一次的定时器：先查盘口数据时间戳，变了才真正去拉盘口 | `MainPage.xaml.cs` 的 `timeTimer` / `FetchTimeAsync` |

---

## 默认不碰的东西

- `bin/`、`obj/`、`.vs/`
- 缓存文件：`%USERPROFILE%\Desktop\UserCache.json`
- 不擅自加 NuGet 包、不升级 .NET 版本、不改目录结构、不改 README

## 汇报方式

先给结论，再给证据（`文件:行`）。不确定的地方直说，不用"应该没问题"糊过去。
